from setuptools import find_packages, setup

setup(
    name="proto",
    version="0.0.1",
    description="인공지능 북커톤 대회 로깅을 위한 패키지",
    python_requires=">=3.7",
    install_requires=[],
    packages=find_packages(),
)
